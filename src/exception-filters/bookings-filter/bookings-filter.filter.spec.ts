import { BookingsExceptionFilter } from './bookings-filter.filter';

describe('BookingsFilterFilter', () => {
  it('should be defined', () => {
    expect(new BookingsExceptionFilter()).toBeDefined();
  });
});

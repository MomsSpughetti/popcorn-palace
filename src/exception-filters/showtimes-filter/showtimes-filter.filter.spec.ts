import { ShowtimesExceptionFilter } from './showtimes-filter.filter';

describe('ShowtimesFilterFilter', () => {
  it('should be defined', () => {
    expect(new ShowtimesExceptionFilter()).toBeDefined();
  });
});
